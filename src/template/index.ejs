<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Navegador Simulado</title>
    <style> 
        body { font-family: sans-serif; padding-left: 10px; padding-right: 10px; }
        .adress-bar { background: #eee; padding: 10px; margin-bottom: 20px; border-radius: 5px; display: flex; }
        input { padding: 5px; width: 300px; }
        button { padding: 5px 10px; cursor: pointer; }
    </style>
</head>
<body>
    <h1>Simulador de Navegador</h1>
    <div class="adress-bar">
        <form action="/back" method="post">
            <button type="submit" <% if (!canBack) { %> disabled <% } %> >&larr;</button>
        </form>
        <form action="/forward" method="post">
            <button type="submit" <% if (!canForward) { %> disabled <% } %> >&rarr;</button>
        </form>
        <form action="/search" method="post">
            <input type="text" name="search" placeholder="Digite para buscar" required>
            <button type="submit">Buscar</button>
        </form>
    </div>
    
    <hr>

    <div class="content">
        <% if (page) { %>
            <h2><%= page.title %></h2>
            <ul>
                <% page.content.forEach(result => { %>
                    <li>
                        <a href="<%= result.url %>"><%= result.title %></a>
                        <p><%= result.snippet %></p>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <div style="text-align: center; color: #888;">
                <h3>Navegador Vazio</h3>
                <p>Navegador vazio. Faça uma pesquisa.</p>
            </div>
        <% } %>
    </div>
</body>
</html>